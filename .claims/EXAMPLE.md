---
agent: example
phase: 0
branch: none
status: template
migrations: false
updated: 2026-09-09
---

## paths

src/ContentPilot.Example/
tests/ContentPilot.UnitTests/Example/

## notes

Copy this file to `<your-name>.md`. `status` is `active` while you are working and `done`
once merged; only `active` claims are enforced. Set `migrations: true` only if you are the
single agent allowed to run `dotnet ef migrations add`.
